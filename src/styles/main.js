import {StyleSheet} from "react-native";

export default StyleSheet.create({
    pageContainer : {
        padding: 4,
        borderWidth: 0,
        borderColor: "green",
        height: "100%"
    }
});